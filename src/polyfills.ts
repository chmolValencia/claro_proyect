//import 'core-js/es/reflect'; // Agrega esta importación
/*import 'zone.js/dist/zone'; // Agrega esta importación
import 'stream'; // Agrega esta importación
import 'crypto-browserify';
import 'stream-browserify';


(window as any).global = window;
(window as any).process = {
  env: { DEBUG: undefined },
};*/
